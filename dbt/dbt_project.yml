name: company_analytics
version: 1.0.0
config-version: 2

profile: company_analytics

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  company_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
